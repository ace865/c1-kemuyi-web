# 项目维护手册

本文档规定题库、依赖、用户数据、发布和故障处理的长期维护流程。

## 维护节奏

| 周期 | 检查内容 |
| --- | --- |
| 每次 PR | 自动测试、删除文件、资源打包、浏览器回归 |
| 每月 | npm 依赖、Electron 安全公告、GitHub Actions 更新 |
| 每季度 | 题库来源与法规变化、失效题目、图片完整性 |
| 每次发布 | 安装、升级、卸载、离线运行和用户数据保留 |

## 题库维护

题库更新必须使用独立 PR，并添加 `approved-question-bank-update` 标签。

更新步骤：

1. 记录数据来源、获取日期、适用地区和授权状态。
2. 保留原始数据备份，不直接覆盖唯一副本。
3. 检查科目、题型、地区、题目 ID、选项和答案格式。
4. 多选答案必须全部存在于选项中，答案顺序不得影响判分。
5. 检查每个图片 URL 都有本地文件，并检查无题目引用的孤立图片。
6. 更新预期题量、资源检查、打包清单和 `NOTICE.md`。
7. 分别运行科目一、科目四顺序练习、图片题和模拟考试。

题库更新不得与 UI 大改、存储迁移或 Electron 升级放在同一个 PR。

## 依赖维护

Dependabot 每月检查 npm 和 GitHub Actions。合并依赖更新前必须：

- 阅读上游变更日志和安全公告；
- 保持 Node.js 22 兼容；
- 对 Electron 大版本升级单独开 PR；
- 验证自定义协议、CSP、沙箱、网络阻断、JSON、JPG、PNG 和 GIF；
- 重新生成并测试 Windows 安装包。

不要为了消除警告一次性升级全部依赖。

## 本地数据与迁移

- 不得复用或随意修改已有 `localStorage` key。
- 数据结构升级必须递增版本并保留旧格式读取路径。
- 迁移必须幂等：重复启动不能重复写入或丢失记录。
- 科目一和科目四的进度、错题、考试记录必须隔离。
- 更新后应保留档案、错题、正确率、顺序位置和未完成考试。
- 删除用户数据必须由用户在界面中明确确认，不得在启动时静默清理。

## 发布流程

版本号遵循语义化版本：

- Patch：兼容的 Bug 修复；
- Minor：兼容的新功能；
- Major：不兼容的数据或行为变更。

发布前：

1. 更新版本号和 `CHANGELOG.md`。
2. 从干净工作区运行 `npm run check` 和 `npm run dist:win`。
3. 在全新系统验证安装、启动、离线运行和卸载。
4. 从上一版本升级，确认本地数据保留。
5. 验证两套题库及所有图片进入安装包。
6. 创建 Git tag 和 GitHub Release，附校验值与已知问题。
7. 保留上一稳定安装包，确保可以回滚。

## 故障处理

1. 记录版本、提交号、平台、复现步骤、截图和日志。
2. 判断故障属于网页、Electron、Android 壳、存储还是题库。
3. 建立最小复现和回归测试。
4. 只修改故障相关部分，不用整体覆盖解决局部问题。
5. 在独立修复分支验证后发布 Patch 版本。
6. 若新版本造成数据丢失、安全问题或无法启动，停止分发并回滚到上一稳定版本。

## 维护责任

- 仓库所有者负责权限、分支保护、版本发布和最终合并。
- 功能作者负责测试证据、迁移说明和剩余风险。
- 审查者重点检查删除文件、存储兼容、动画保留、资源数量和打包清单。
- AI 只能执行已授权的修改，不能替代维护者批准破坏性操作。
