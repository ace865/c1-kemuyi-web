# 安全政策

## 支持范围

安全修复只面向 `main` 和最新 GitHub Release。旧安装包可能不会单独回补修复。

## 报告漏洞

请优先使用仓库 **Security** 页面中的 **Report a vulnerability** 私密报告功能。若该入口不可用，请先通过维护者 GitHub 主页提供的私人联系方式沟通。

不要在公开 Issue 中发布令牌、个人数据、完整利用代码或可直接复现攻击的敏感细节。

报告应包含：

- 受影响版本和提交号；
- Windows、浏览器或 Android 环境；
- 复现步骤和影响范围；
- 最小证明材料；
- 已尝试的缓解措施。

## 安全边界

项目应持续保持：

- Electron `contextIsolation`、`sandbox` 与禁用 Node.js 注入；
- 固定本地协议和路径越界防护；
- 默认阻断非必要远程网络请求；
- 严格 CSP，不加载远程脚本；
- 用户学习数据只保存在本机；
- 不在源码、日志、Issue 或安装包中保存凭据。

任何降低这些限制的修改都必须单独提交并经过维护者审查。
